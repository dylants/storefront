<%= f.input :name %>
<%= f.input :item_type,
            :label => t('.type'),
            :collection => [t('taco'), t('hot_sauce'), t('chips')],
            :include_blank => false %>
<%= f.input :description, :input_html => { :rows => 3 } %>
<%= f.input :price %>
